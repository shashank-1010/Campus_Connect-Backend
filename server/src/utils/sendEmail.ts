// Email utility placeholder — integrate nodemailer or similar when needed
export const sendEmail = async (to: string, subject: string, body: string): Promise<void> => {
  console.log(`[Email] To: ${to} | Subject: ${subject} | Body: ${body}`);
};
